<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.game.mapper.MemberMapper">
  <select id="memberList" resultType="memberListModel">
    select a.id,a.account,a.username,a.head_image,a.disable,a.balance,a.create_time,b.name as company_name from member
    as a left join company as b on a.company_id=b.id
    <where>
      <if test=" account != null and account !=''">
        and a.account like concat('%',#{account},'%')
      </if>
      <if test=" mobile != null">
        anda. mobile like concat('%',#{mobile},'%')
      </if>
      <if test=" username != null and username !=''">
        and a.username like concat('%',#{username},'%')
      </if>
      <if test=" disable != null ">
        and a.disable=#{disable}
      </if>
    </where>
    order by a.id desc
  </select>
</mapper>